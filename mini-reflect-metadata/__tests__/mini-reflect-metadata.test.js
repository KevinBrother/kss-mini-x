'use strict';

const miniReflectMetadata = require('..');
const assert = require('assert').strict;

assert.strictEqual(miniReflectMetadata(), 'Hello from miniReflectMetadata');
console.info('miniReflectMetadata tests passed');
